---
- hosts: localhosts
  connection: local
  tasks:
  -  name: "install programs on virtual machine"
    apt:
      pkg:
      - python3
      - code
      - docker
      - docker-compose
      state: latest
  - name: "pull repo to add this playbook repository"
    git:
      repo: "https://github.com/2206-devops-batch/JoseR-project0"
      dest: /src/projects
      version: master

